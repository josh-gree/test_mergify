How we could automate the `feature` -> `staging` -> `master` git flow using `mergify` and `hub`.

## TODO

- How to actually trigger the dag automaticaly and check it succeded? What security concerns may there be?
